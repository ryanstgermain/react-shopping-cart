import React from 'react'
import '../style/index.css';

const AddItem = (props) => {
    const AddedItem = props.products.map(product => {
        return (
        
        )
    })
        return (
            <div className="collection-item">{AddedItem}</div>
        )
}

export default AddItem;
